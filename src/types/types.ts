export enum ConversationState {
  UNSPECIFIED = "UNSPECIFIED",
  MAIN_MENU = "MAIN_MENU",
  FLOW = "FLOW",
}

export enum Countries {
    FRANCE = "FRANCE",
}

export enum City {
    PARIS = "PARIS",
}